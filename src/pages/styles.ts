import styled from 'styled-components'

import { bgColors } from 'styles/theme/color'

export const Container = styled.div`
  position: absolute;
  width: 100%;
  height: 100%;
  background-color: ${bgColors.white};
`
